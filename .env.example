# Git repository to clone into the workspace
# Leave empty to skip automatic cloning
# Use SSH format (git@github.com:user/repo.git) to use your SSH keys
GIT_REPO_URL=

# Optional: Specify a branch to checkout (defaults to main/master)
# GIT_BRANCH=main

# Optional: Directory name for the cloned repo (defaults to repo name from URL)
# GIT_CLONE_DIR=my-project

# LLM CLI Selection
# Choose which LLM CLI to use: "claude" or "codex"
# Default: claude
# Note: Use ./cc-start for Claude Code instances, ./codex-start for Codex instances
LLM_TYPE=claude

# Claude Code runtime flags
# Set to "true" to enable --dangerously-skip-permissions flag
# This bypasses all permission checks - use only in trusted sandboxes with no internet access
CLAUDE_SKIP_PERMISSIONS=false

# OpenAI API Configuration (for Codex CLI)
# Required when using LLM_TYPE=codex
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your_openai_api_key_here

# Codex CLI runtime flags
# Option 1: Full YOLO mode (bypasses ALL approvals and sandboxing)
# Set to "true" to enable --yolo flag - use only in hardened setups like Docker
CODEX_YOLO=false

# Option 2: Just disable approval prompts (keeps sandboxing)
# Set to "true" to enable --ask-for-approval never flag
CODEX_NO_APPROVAL=false

# Note: Persistent configuration also available in codex-data/config.toml
# See README.md for details on approval_policy and sandbox_mode settings

# GitHub Authentication
# Optional: GitHub Personal Access Token for authenticating gh CLI
# Required for creating PRs and other GitHub operations
# Create a token at: https://github.com/settings/tokens (needs 'repo' scope)
# GITHUB_TOKEN=ghp_your_token_here

# User ID and Group ID for container permissions
# These are automatically set by build.sh to match your host user
# This ensures proper file permissions for mounted volumes
USER_ID=1000
GROUP_ID=1000
